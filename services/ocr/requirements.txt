fastapi==0.128.8
uvicorn==0.39.0
httptools==0.6.4
uvloop==0.21.0; sys_platform != 'win32'
SQLAlchemy==2.0.48
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic-settings==2.9.1
pytesseract==0.3.13
pdfplumber==0.11.4
Pillow==11.2.1
opencv-python-headless==4.11.0.86
numpy>=1.26.0
python-docx==1.1.2
openpyxl==3.1.5
pdf2image==1.17.0
paddlepaddle==3.2.1; sys_platform != 'win32'
paddleocr[doc-parser]>=3.0.0

# ── Advanced format support (PR #45) ────────────────────────────────────────
# All optional at runtime — engine.py guards each import with _HAS_* flags.
pillow-heif>=0.16.0           # HEIC / HEIF photos from iPhones
python-pptx==1.0.2            # PowerPoint decks
odfpy==1.4.1                  # OpenDocument (.odt / .ods / .odp)
striprtf==0.0.27              # Rich Text Format
pikepdf>=9.0.0                # decrypt password-protected PDFs
# Optional layout-aware PDF parser. Heavy (~1 GB models). Opt-in via OCR_USE_DOCLING=true.
# pip install docling>=2.0
celery==5.4.0
redis==5.2.1
