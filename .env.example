# =====================================================
# ClaimGPT Environment Variables
# Copy to .env and customize
# =====================================================

# ---- Database
DATABASE_URL=postgresql://claimgpt:claimgpt@postgres:5432/claimgpt

# ---- MinIO (Object Storage)
MINIO_ROOT_USER=claimgpt
MINIO_ROOT_PASSWORD=claimgpt123
MINIO_ENDPOINT=http://minio:9000

# ---- Auth (Keycloak)
AUTH_ENABLED=false
AUTH_JWKS_URL=http://keycloak:8080/realms/claimgpt/protocol/openid-connect/certs
AUTH_JWT_AUDIENCE=claimgpt
AUTH_JWT_SECRET=dev-secret-change-in-production

# ---- Observability
OTEL_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
METRICS_ENABLED=false

# ---- Chat / LLM
CHAT_LLM_URL=http://llm:8888/v1/chat/completions
CHAT_LLM_MODEL=llama-3-8b

# ---- Service URLs (for workflow orchestrator)
WORKFLOW_OCR_URL=http://ocr:8000
WORKFLOW_PARSER_URL=http://parser:8000
WORKFLOW_CODING_URL=http://coding:8000
WORKFLOW_PREDICTOR_URL=http://predictor:8000
WORKFLOW_VALIDATOR_URL=http://validator:8000

# ---- Submission
FHIR_ENDPOINT=
FHIR_AUTH_TOKEN=
X12_ENDPOINT=
SUBMISSION_DEFAULT_PAYER=generic

# ---- Logging
LOG_LEVEL=INFO

# ---- OpenRouter (primary LLM for expense extraction + diagnosis coding)
# Set your OpenRouter API key and model here
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
OPENROUTER_URL=https://openrouter.ai/api/v1/chat/completions

# Concurrency limit for parsing regions with LLM (set to 1 or 2 locally to avoid 429 rate limit storms on free keys)
PARSER_SEMANTIC_CONCURRENCY=2

# OpenRouter backend ordering (primary is openrouter, followed by local VLM fallbacks)
PARSER_SEMANTIC_BACKEND_ORDER=openrouter,qwen2-vl,layoutlmv3,florence-2,donut

# ---- Gemini (immediate fallback when OpenRouter is rate-limited)
# Set your Gemini API key and model here
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash-lite

